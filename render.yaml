# Render Blueprint for ICEWISE Routing Backend
services:
  - type: web
    name: icewise-backend
    env: python
    buildCommand: pip install -r backend/routing/requirements.txt
    startCommand: uvicorn backend.routing.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: CORS_ORIGINS
        value: "*"
